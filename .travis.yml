language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-node-6to5' ]; then cd .. && eval "mv $currentfolder generator-node-6to5" && cd generator-node-6to5; fi
